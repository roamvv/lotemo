info:
  name: Add Property to Collection
  type: http
  seq: 3

http:
  method: POST
  url: "{{serverUrl}}/api/v1/collections/44012/properties"
  body:
    type: json
    data: |-
      {
        "propertyId": 3391077
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
