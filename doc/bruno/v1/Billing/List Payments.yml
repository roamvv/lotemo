info:
  name: List Payments
  type: http
  seq: 5

http:
  method: GET
  url: "{{serverUrl}}/api/v1/agencies/7841002/payments"
  params:
    - name: "purpose"
      value: "CHARGE"
      type: query
    - name: "status"
      value: "SUCCEED"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
