info:
  name: Mark All Read
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{serverUrl}}/api/v1/me/notifications/read-all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
