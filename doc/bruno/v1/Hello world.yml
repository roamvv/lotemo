info:
  name: Hello world
  type: http
  seq: 1

http:
  method: GET
  url: "{{serverUrl}}/api/v1/hello"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
