info:
  name: List Price History
  type: http
  seq: 2

http:
  method: GET
  url: "{{serverUrl}}/api/v1/properties/3391077/prices"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Returns the full chain of PropertyPrice rows (each references prevPriceId) so the frontend can show a price-drop badge.
