info:
  name: Get Current Price
  type: http
  seq: 1

http:
  method: GET
  url: "{{serverUrl}}/api/v1/properties/3391077/price"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
