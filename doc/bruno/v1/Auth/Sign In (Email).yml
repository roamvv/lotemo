info:
  name: Sign In (Email)
  type: http
  seq: 2

http:
  method: POST
  url: "{{serverUrl}}/api/v1/auth/sign-in/email"
  body:
    type: json
    data: |-
      {
        "email": "iain.cruz@meridianrealty.ph",
        "password": "Sup3rSecret!23"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  On success Better Auth sets the session cookie and returns the session token.
  Subsequent requests in this collection rely on that cookie via `auth: inherit`.
