info:
  name: Get Session
  type: http
  seq: 3

http:
  method: GET
  url: "{{serverUrl}}/api/v1/auth/get-session"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
