info:
  name: Sign Out
  type: http
  seq: 4

http:
  method: POST
  url: "{{serverUrl}}/api/v1/auth/sign-out"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
