info:
  name: Sign Up (Email)
  type: http
  seq: 1

http:
  method: POST
  url: "{{serverUrl}}/api/v1/auth/sign-up/email"
  body:
    type: json
    data: |-
      {
        "name": "Iain Cruz",
        "email": "iain.cruz@meridianrealty.ph",
        "password": "Sup3rSecret!23"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Registers a new user account. Better Auth creates the iam_users + iam_accounts rows.
  Use this to create an agent account before adding them to an agency.
