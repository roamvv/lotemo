info:
  name: List Organizations
  type: http
  seq: 6

http:
  method: GET
  url: "{{serverUrl}}/api/v1/auth/organization/list"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
