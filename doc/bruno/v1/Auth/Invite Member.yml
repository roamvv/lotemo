info:
  name: Invite Member
  type: http
  seq: 7

http:
  method: POST
  url: "{{serverUrl}}/api/v1/auth/organization/invite-member"
  body:
    type: json
    data: |-
      {
        "email": "grace.tan@meridianrealty.ph",
        "role": "member",
        "organizationId": "org_9f21ab7c"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
