info:
  name: List Unit Types
  type: http
  seq: 1

http:
  method: GET
  url: "{{serverUrl}}/api/v1/projects/44210/unit-types"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Curated unit-type facets shown on the project page (Studio, 1 Bedroom, 2 Bedrooms…), distinct from live-inventory aggregates.
