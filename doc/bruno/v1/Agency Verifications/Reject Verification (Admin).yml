info:
  name: Reject Verification (Admin)
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{serverUrl}}/api/v1/admin/verifications/551201/reject"
  body:
    type: json
    data: |-
      {
        "reason": "PRC license number does not match the registry record."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
