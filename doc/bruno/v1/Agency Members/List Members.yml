info:
  name: List Members
  type: http
  seq: 1

http:
  method: GET
  url: "{{serverUrl}}/api/v1/agencies/7841002/members"
  params:
    - name: "role"
      value: "AGENT"
      type: query
    - name: "status"
      value: "ACTIVE"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
