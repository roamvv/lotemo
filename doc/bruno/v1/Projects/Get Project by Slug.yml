info:
  name: Get Project by Slug
  type: http
  seq: 2

http:
  method: GET
  url: "{{serverUrl}}/api/v1/projects/solinea"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Includes aggregated stats (price range, rent range, bed/bath/parking/floor-area ranges, for-sale vs for-rent counts) computed live from child Property + PropertyPrice rows.
