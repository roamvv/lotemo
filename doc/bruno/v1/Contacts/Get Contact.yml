info:
  name: Get Contact
  type: http
  seq: 3

http:
  method: GET
  url: "{{serverUrl}}/api/v1/contacts/60211"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
