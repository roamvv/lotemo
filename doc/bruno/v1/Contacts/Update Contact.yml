info:
  name: Update Contact
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{serverUrl}}/api/v1/contacts/60211"
  body:
    type: json
    data: |-
      {
        "phone": "+639175559876"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
