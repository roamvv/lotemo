info:
  name: Get Location
  type: http
  seq: 2

http:
  method: GET
  url: "{{serverUrl}}/api/v1/locations/154"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
