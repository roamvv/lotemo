info:
  name: Search Locations
  type: http
  seq: 3

http:
  method: GET
  url: "{{serverUrl}}/api/v1/locations/search"
  params:
    - name: "q"
      value: "Makati"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Typeahead used by the property search bar, e.g. 'Search a city or barangay'.
