info:
  name: List Saved Properties
  type: http
  seq: 1

http:
  method: GET
  url: "{{serverUrl}}/api/v1/me/saved-properties"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
