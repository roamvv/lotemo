info:
  name: List Saved Searches
  type: http
  seq: 4

http:
  method: GET
  url: "{{serverUrl}}/api/v1/me/saved-searches"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
