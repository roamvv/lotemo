info:
  name: Get Agency Stats
  type: http
  seq: 5

http:
  method: GET
  url: "{{serverUrl}}/api/v1/agencies/7841002/stats"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Dashboard summary: activeListingCount, viewsCount, totalClosedTransactionCount, leads this month, etc.
