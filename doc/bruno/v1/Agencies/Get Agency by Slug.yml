info:
  name: Get Agency by Slug
  type: http
  seq: 2

http:
  method: GET
  url: "{{serverUrl}}/api/v1/agencies/meridian-realty-ph"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
