info:
  name: Get Nearest Office
  type: http
  seq: 6

http:
  method: GET
  url: "{{serverUrl}}/api/v1/properties/3391077/nearest-office"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Orders ag_offices by distance from the property's gcsId point, same pattern as the Property/Location proximity queries.
