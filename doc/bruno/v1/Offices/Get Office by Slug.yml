info:
  name: Get Office by Slug
  type: http
  seq: 5

http:
  method: GET
  url: "{{serverUrl}}/api/v1/offices/palawan"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
