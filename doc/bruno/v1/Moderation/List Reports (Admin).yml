info:
  name: List Reports (Admin)
  type: http
  seq: 2

http:
  method: GET
  url: "{{serverUrl}}/api/v1/admin/moderation/reports"
  params:
    - name: "status"
      value: "PENDING"
      type: query
    - name: "targetType"
      value: "PROPERTY"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
