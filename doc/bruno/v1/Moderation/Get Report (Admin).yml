info:
  name: Get Report (Admin)
  type: http
  seq: 3

http:
  method: GET
  url: "{{serverUrl}}/api/v1/admin/moderation/reports/6620"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
