info:
  name: List Conversation Messages
  type: http
  seq: 1

http:
  method: GET
  url: "{{serverUrl}}/api/v1/conversations/conv_28870/messages"
  params:
    - name: "page"
      value: "1"
      type: query
    - name: "perPage"
      value: "50"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
